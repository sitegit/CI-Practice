name: Android CI Build

on:
  push:
    branches: [ "main", "master", "develop", "dev" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # Создаем файл google-services.json
      - name: Decode Google Services JSON
        env:
          GOOGLE_SERVICES_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}
        run: |
          echo "$GOOGLE_SERVICES_BASE64" | base64 --decode > app/google-services.json

      # ==========================================
      # БЛОК ДЛЯ DEBUG (Ветка develop / dev)
      # ==========================================

      - name: Build Debug APK
        if: github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/dev'
        run: ./gradlew assembleDebug

      # Отправляем Debug APK в Firebase!
      - name: Upload Debug APK to Firebase
        if: github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/dev'
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          groups: testers
          file: app/build/outputs/apk/debug/*.apk

      # ==========================================
      # БЛОК ДЛЯ RELEASE (Ветка main / master)
      # ==========================================

      - name: Decode Keystore
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 --decode > app/release.keystore

      # СОБИРАЕМ СРАЗУ И APK (для Firebase) И AAB (для Google Play)
      - name: Build Release APK and AAB
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        env:
          RELEASE_STORE_FILE: release.keystore
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: ./gradlew assembleRelease bundleRelease

      # Сохраняем AAB в артефакты для ручной заливки в Google Play
      - name: Upload Release AAB to Artifacts
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: actions/upload-artifact@v4
        with:
          name: app-release-bundle
          path: app/build/outputs/bundle/release/*.aab

      # Отправляем Release APK в Firebase!
      - name: Upload Release APK to Firebase
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          groups: testers
          file: app/build/outputs/apk/release/*.apk
